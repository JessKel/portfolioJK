{% extends 'base-dashboard.html.twig' %}

{% block title %}Message{% endblock %}

{% block body %}
    <h1 class="jk-title-admin mb-5">Message</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ message.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ message.email }}</td>
            </tr>
            <tr>
                <th>Subject</th>
                <td>{{ message.subject }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ message.message }}</td>
            </tr>
        </tbody>
    </table>
    
    <a class="retour1" href="{{ path('message_index') }}">Retour à la liste des messages</a>

    {{ include('message/_delete_form.html.twig') }}
{% endblock %}
