{% extends 'base-dashboard.html.twig' %}

{% block title %}Edit roles
{% endblock %}

{% block body %}
<div class="container form-jk-admin">
	<h1 class="jk-title-admin mb-5">Changer le rôle de l'utilisateur</h1>
		<div class="centered-container">
			{{ form_start(form) }}
			{{ form_widget(form) }}
		<button class="btn">{{ button_label|default('Enregistrer') }}</button>
			{{ form_end(form) }}
		</div>
</div>
{% endblock %}